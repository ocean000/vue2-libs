/**
 * Created by Ocean on 2017/9/9.
 */

export default {
    /**
     * 创建vuex的store实例
     * @param state state的名称
     * @returns 返回store实例
     */
    createStore(state) {
        return {
            state: state
        }
    }
}
